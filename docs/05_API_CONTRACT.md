# 05 — API Contract

**Purpose:** Specifies all REST/GraphQL endpoints, request/response schemas, error codes, authentication, and rate-limiting rules. The contract between frontend and backend teams.
